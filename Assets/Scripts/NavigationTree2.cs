

using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using DataStructures;


  

    /*
    public class NavigationTreeGrid
    {
        Dictionary<TreePoint,GameNode> nodeByPos;
        Dictionary<TreePoint,Variation> variationByPos;
        public Variation IsInVariation(TreePoint p)
        {
            if(variationByPos[p] != null)
            {
                return variationByPos[p]; 
            }
            return null;
        }
        public void AddVariation(Variation v)
        {

        }
        
        public bool NextNode()
        {

        current.GetChildrens[childIndex];
        }




        public void CreateTree(GameNode node,Variation currentVar)
        {
            List<GameNode> childrens = node.GetChidrens();
            node.SetVariation(currentVar);
            if(childrens.Count >0)
            {
                InitVariations(childrens[0],currentVar);
                for(int i=1; i<childrens.Count ;i++)
                {
                    InitVariations(childrens[i],new Variation(node));
                }
            }
        }
    }
*/
    
